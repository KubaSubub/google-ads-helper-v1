## Raport PM — 2026-04-22

### Faktyczny stan aplikacji
- Endpointy: 191 (zweryfikowane grep `@router.get/post/patch/delete` + git log)
- Strony frontend: 27 tras (19 unikalnych stron + 4 redirect + 3 hidden/utility), 25+ w nawigacji
- Serwisy: 35 plików w `app/services/` (11 mixinów analytics w `services/analytics/`)
- Tabele DB: 42 modeli (`backend/app/models/`)
- Testy: **808 collected** (PROGRESS.md mówi 806 — różnica 2, prawdopodobnie 2 nowe testy dodane po docs-sync)

### Status feature'ów (na podstawie KODU)

| Feature | Endpoint | UI | Test | Status |
|---------|----------|----|------|--------|
| Analytics refactor ADR-021 | 15 sub-routerów + `_legacy.py` (0 dekoratorów) | — | 127 analytics tests | OK |
| Dayparting DoW | `_pacing.py /dayparting` | `DayOfWeekWidget` → Dashboard + AuditCenter | `test_analytics_endpoints.py` | OK |
| Hourly dayparting | `_pacing.py /hourly-dayparting` | `HourlyDaypartingWidget` → Dashboard | `test_hourly_dayparting_response_shape` | OK |
| 7×24 heatmap | `_pacing.py /dayparting-heatmap` | `DowHourHeatmapWidget` → Dashboard; `DowHourHeatmapSection` → AuditCenter | test_analytics_endpoints.py | OK |
| DoW bid suggestions | `_pacing.py /dayparting-dow-suggestions` | przez useAuditData | test_analytics_endpoints.py | OK |
| Hourly bid suggestions | `_pacing.py /dayparting-hourly-suggestions` | przez useAuditData | test_analytics_endpoints.py | OK |
| TrendExplorer fullscreen | — | `TrendExplorer.jsx` portal | e2e/trend-explorer.spec.js | OK |
| Dashboard KPIs cache | `_kpis.py /dashboard-kpis` | DashboardPage debounce | test_dashboard_kpis.py | OK |
| Health score cost-weighted | `_health.py /health-score` | — | test_analytics_endpoints.py (3 testy) | OK |
| CommonFilters contract | `/campaigns`, `/keywords`, `/analytics/dashboard-kpis` | — | test_common_filters.py, test_filters_contract.py | CZESCIOWO (~35 endpointów jeszcze niezmigrowanych) |
| Startup sync | `startup_sync.py` + `main.py` lifespan | SyncModal (MCC Overview only) | — | OK (brak dedykowanego testu startup_sync) |
| Hourly MetricSegmented sync z API | `google_ads.py` — tylko filtr `hour_of_day.is_(None)` | DowHourHeatmap + Hourly widgets | — | CZESCIOWO — brak fazy hourly w PHASE_REGISTRY; dane z seed/backfill skryptu |

### Dokumentacja vs rzeczywistosc

| Dokument | Problem | Szczegoly |
|----------|---------|-----------|
| `docs/API_ENDPOINTS.md` | NIEAKTUALNY (minor) | 1 unstaged zmiana — komentarz o sub-routerach aktualizuje liczby; nie commitowany. Zawartość endpointów zgodna z kodem. |
| `PROGRESS.md` | STALE ITEMS | Linie 42-43: `[ ]` dla `git rm .bak` i commit ADR-021 — faktycznie już wykonane (commit `5d1a1a8` z 2026-04-22). PROGRESS.md nie był zaktualizowany po wykonaniu tych zadań. |
| `PROGRESS.md` | STALE ITEMS | Linie 77-78: `[ ]` `/quality-score-audit` migracja i decyzja o duplikacie — oba WYKONANE: quality-score-audit jest w `_quality.py` (linia 24), `/shopping-product-groups-tree` istnieje. |

### Pliki do usunięcia (blokowane przez hook, wymienione w PROGRESS.md)

| Plik | Typ | Status |
|------|-----|--------|
| `backend/_split_analytics_service.py` | One-off generator skryptu | Untracked — do `git rm` |
| `backend/app/services/analytics_service.py.bak` | Pre-split backup 243 KB | Untracked — do `git rm` |
| `backend/backfill_hourly_segments.py` | Utility script (idempotent backfill) | Untracked — do `git rm` gdy hourly sync zaimplementowane w PHASE_REGISTRY |

### TODO/FIXME/HACK

Brak (grep po `backend/app/` i `frontend/src/` — czyste).

### Otwarte ryzyka

- SREDNIE: `backfill_hourly_segments.py` wskazuje, że **hourly MetricSegmented dane są z seeda/backfill, nie z Google Ads API**. `HourlyDaypartingWidget` i `DowHourHeatmapWidget` będą puste dla nowych klientów bez ręcznego backfill. Brak fazy `hourly_metrics` w `sync_config.PHASE_REGISTRY`.
- SREDNIE: `CommonFilters` contract niezmigrowany dla ~35 endpointów — naruszenie ADR konwencji, ale `test_filters_contract.py` gatuje tylko zarejestrowane `DATA_ENDPOINTS`.
- NISKIE: `docs/API_ENDPOINTS.md` ma unstaged zmianę. Nie jest krytyczne (zawartość endpointów poprawna), ale git status jest "brudny".
- NISKIE: `PROGRESS.md` zawiera przestarzałe `[ ]` checkboksy dla zadań już wykonanych (lines 42-43, 77-78). Dezorientuje przy kolejnym sesji.
- NISKIE: `services/scheduler.py` + `routers/scheduled_sync.py` to dead code (ADR-020) — celowo zatrzymane, nie ryzyko, ale klarowność jest obniżona.

### Rekomendacje

1. `git rm backend/_split_analytics_service.py backend/app/services/analytics_service.py.bak` — oba są garbage z refaktoru.
2. Zaktualizować `PROGRESS.md` — zamknąć checkboksy na liniach 42-43 i 77-78 (już wykonane).
3. Zaplanować `hourly_metrics` fazę w `sync_config.PHASE_REGISTRY` przed v1.1 — bez tego HourlyDaypartingWidget jest martwy dla prawdziwych klientów.
4. Commitować `docs/API_ENDPOINTS.md` (jedyna niestaged zmiana).

### Ocena ogolna: 8/10

Dobry stan. Architektura analytics (ADR-021) zrealizowana wzorowo — 191 endpointów, 808 testów, zero TODO/FIXME w kodzie produkcyjnym. Główne ryzyka to: hourly data gap (widget widoczny ale bez danych dla realnych klientów) i stale PROGRESS.md checkboksy. Brak krytycznych bugów.
